services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vitae
      POSTGRES_PASSWORD: vitae
      POSTGRES_DB: vitae
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
